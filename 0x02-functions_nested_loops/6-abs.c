#include "main.h"

/**
 * _abs - This function computes the absolute value of an integer
 *
 * @n: This the function parameter
 *
 * Return: Always return 0 (Success)
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
